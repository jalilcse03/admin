<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bubtjobs.com.admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".admin_home"
            android:label="@string/app_name" />
        <activity
            android:name=".RegistrationList_activity"
            android:label="@string/title_activity_registration_list_activity" />
        <activity android:name=".Login_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registration" />
        <activity android:name=".StudentHome_activity" />
        <activity android:name=".NotificationSendAdmin" />
        <activity android:name=".NotificationReceiveStudent" />
        <activity android:name=".Exam" />
        <activity android:name=".ExamRule" />
        <activity android:name=".ExamDuration"></activity>
    </application>

</manifest>
